/**
 * Wordings
 */
const APP_NAME = 'overtime';
const DESCRIPTION = "torneo de básquet";
const EDITION = `EDICION ${new Date().getFullYear()} - ARGENTINA`;

export default {
    APP_NAME,
    DESCRIPTION,
    EDITION
};
